# Completion Report — Documentation: Release Notes & Changelog (release-notes.md, 5 languages)

This task's plan: [docs/plans/20260908_documentacao_release_notes.md](../../../plans/20260908_documentacao_release_notes.md) · Feature sources: [ADR-002-gitpr-release-subcommand.md](../../../plans/ADR-002-gitpr-release-subcommand.md) · [ADR-003-gitpr-release-flat-modules.md](../../../plans/ADR-003-gitpr-release-flat-modules.md) · [glossary-release-notes.md](../../../plans/glossary-release-notes.md) · [20260904_skill_gitpr_release_notes_spec.md](../../../plans/20260904_skill_gitpr_release_notes_spec.md) · Surveys: [20260907_gitpr_release_notes_surveyfacts.md](../../../survey/20260907_gitpr_release_notes_surveyfacts.md) · [20260908_skill_release_template_surveyfacts.md](../../../survey/20260908_skill_release_template_surveyfacts.md) · Feature reports: [2026-09-07_gitpr_release_feature.md](2026-09-07_gitpr_release_feature.md) · [2026-09-07_gitpr_release_notes_grill.md](2026-09-07_gitpr_release_notes_grill.md) · [2026-09-08_skill_release_template.md](2026-09-08_skill_release_template.md)

## What was done

Created the missing user-facing technical documentation family for the **GitPR Automated Changelog / Release Notes** feature (`gitpr release` — the CLI's first subcommand) in `docs/`, mirroring the conventions of the existing 31 doc families (model: `docs/code-review-ia.md`) — English master without suffix plus the four language copies (`.pt_br.md`, `.pt_pt.md`, `.es_es.md`, `.fr_fr.md`) with line-for-line parity (all five files: **230 lines each**, headings at identical line numbers). The family name `release-notes.md` was chosen by the user via a grilling question — it is not imposed by code, since the subcommand has no `HELP_MAP`/`get_doc_url` entry yet (unlike the suggested-reviewers precedent). All content was fidelity-verified against the source code before writing (`src/main.py` release subcommand region, `src/config.py` defaults, `src/changelog_builder.py`, `src/commit_classifier.py`), with anti-overclaim guardrails (no interactive TUI preview in v1 — terminal preview capped at 40 lines; no range between two tags; no YAML config; quoted UI strings kept verbatim English — the changelog headings and release-flow messages go through the `__()` engine with English fallback). Dated plan in `docs/plans/` plus this mandatory completion report. No ADR was created (no hard-to-reverse decision); ADR-002/ADR-003/glossary/spec/surveys and the feature reports were left untouched.

- **`docs/release-notes.md`** (EN master, 230 lines): `# Technical Documentation: Release Notes & Changelog (gitpr release)` with numbered sections — 1. Overview (intro on the subcommand + 1.1 Command Reference: the 6 options `--since`/`--version`/`--publish`/`--draft`/`--format {markdown|json}`/`--force` with their verbatim CLI help strings, plus a default-flow characteristics table: range `since..HEAD` without merges, automatic AI summary, files written, nothing published, tags/version files never touched); 2. Release Range and Version Suggestion (2.1 Commit Range — default origin `git describe --tags --abbrev=0`, no-tag = first release from the first commit, merges excluded, end always `HEAD`; 2.2 Semantic Version Suggestion — MAJOR/MINOR/PATCH rules, read-only tag-derived suggestion with `v` prefix preserved, `❓ Use the suggested version {version}?` prompt and when it is skipped; 2.3 Explicit Version — sole source of the published tag); 3. Changelog Structure and Files (3.1 Commit Classification — 8-category table with emoji headings, non-conforming → OTHER, PR dedupe, fixed block order, labels via the localization engine; 3.2 Version Section Anatomy — realistic markdown sample of `## [x.y.z] - date` + Summary + category blocks + `**Contributors:**`; 3.3 Files Written — `CHANGELOG.md` (repo root, deliberate exception, `GITPR_RELEASE_CHANGELOG_PATH` override), per-run artifact `.gitpr/reports/release/{branch}_{datetime}_RELEASE.md` best-effort, 40-line terminal preview); 4. AI Executive Summary (4.1 First-Run Skill Template `.gitpr.release.md` — language-aware auto-download that never overwrites and never fails on network errors, skipped in JSON mode, system instruction with **Release Manager** persona; 4.2 Generation and Graceful Degradation — Map-Reduce over 200-commit batches, MD5 cache keyed on the prompt, non-blocking `AI summary failed:` degradation, `GITPR_RELEASE_AI_SUMMARY=false`); 5. Publishing to the Forge (5.1 Confirmation and Guardrails — explicit confirmation, release body without the `## [x.y.z]` heading, no-`origin` guard with exit 1, JSON-mode warning; 5.2 Supported Forges table — GitHub auto-tag on the default branch + drafts, GitLab pre-existing tag, Bitbucket/Azure unsupported with local completion; 5.3 Drafts — GitHub-only concept, draft-by-default); 6. JSON Mode and Idempotency (6.1 Pure JSON Output — stdout-only, field-by-field payload; 6.2 Existing Section and `--force` — exit 1 without touching the file, regeneration override); 7. Environment Variables (the 5 keys with verbatim defaults from `src/config.py` and the "false disables" convention). Closing blockquote cross-links `skill-template.md`; intra-doc links to Section 6 are plain text (translated headings would break GitHub anchors).
- **Four translations** (`pt_br`, `pt_pt`, `es_es`, `fr_fr`): faithful per-language prose (PT-PT genuinely European — "por omissão", "secção", "ficheiro", "artefacto", "rutura"; ES — "por defecto", "archivo", "borrador", "rama por defecto", "forge"; FR with French typography " : " — "par défaut", "fichier", "brouillon", "plage de commits", "notes de version"), headings translated (`# Documentação Técnica: Notas de Versão e Changelog (gitpr release)` / `# Documentación Técnica: ...` / `# Documentation technique : Notes de version et changelog (gitpr release)`), technical tokens verbatim (env vars, flags, paths, code-block commands), quoted UI strings kept verbatim English in all copies (never invented translations — the docs state the changelog headings follow the interface language with English fallback), bash code-block comments translated per the sibling convention while the fenced markdown sample stays byte-identical to the master (it represents the builder's real current output), cross-links kept unsuffixed exactly like the existing translated families.
- **`docs/plans/20260908_documentacao_release_notes.md`**: dated task plan (PT-BR, sibling-plan style) recording context (feature artifacts all still in the working tree, nothing committed), grilling decisions (family name chosen by the user; user-facing reference scope without a For-Developers section; anti-overclaim list), deliverables, document outline, steps and verification.
- **This report**.

### Changed files

| File | Change type | Description |
|------|-------------|-------------|
| docs/release-notes.md | docs | New EN master — Release Notes & Changelog user reference (230 lines) |
| docs/release-notes.pt_br.md | docs | New PT-BR translation (230 lines, line-parity) |
| docs/release-notes.pt_pt.md | docs | New PT-PT translation (230 lines, line-parity) |
| docs/release-notes.es_es.md | docs | New ES translation (230 lines, line-parity) |
| docs/release-notes.fr_fr.md | docs | New FR translation (230 lines, line-parity) |
| docs/plans/20260908_documentacao_release_notes.md | docs | Dated task plan (PT-BR) |
| docs/claude-code/reports/develop_natan/2026-09-08_documentacao_release_notes.md | docs | This report |

### Verification

| Check | Result |
|-------|--------|
| Line parity across the 5 doc files (`wc -l`) | 230 / 230 / 230 / 230 / 230 |
| Heading outline parity (`grep '^#'`, real headings) | Identical line positions in all 5 files (H1 at 1; H2 at 7, 40, 86, 140, 161, 197, 218; 14 H3s aligned; the 3 `#` bash-comment lines at 47/50/147 align in position but differ in content — they are translated, per convention) |
| Fenced code blocks vs master | Byte-identical commands and the full markdown sample in all 4 copies; only the 3 bash comment lines per copy differ (translated, per sibling convention) |
| Technical tokens preserved | Spot-checked: `GITPR_RELEASE_*`, `OUTPUT_FILE_NAME_RELEASE`, `--publish`, `--format json`, `.gitpr.release.md`, `{branch}_{datetime}_RELEASE.md`, skill/provider/scm cross-links intact and unsuffixed in all copies |
| Fidelity to code | Option table == `gitpr release` help strings (`src/main.py`); env defaults == `src/config.py` (`CHANGELOG.md`, `true`, `true`, `true`, `{branch}_{datetime}_RELEASE.md`); section anatomy == `src/changelog_builder.py` output (headings/order/emoji, `**Contributors:**`, clean subjects with scope) |
| Git state | Only the 7 intended new files untracked; nothing staged or committed |

No test suite run — docs-only task, no code touched.

## Impact

- **Functionality:** none — documentation only. Users now have the feature reference that was missing from `docs/`, in the same 5-language family format as every other GitPR technical doc (family #32).
- **Performance:** none.
- **Compatibility:** purely additive. Existing ADR-002/ADR-003/glossary/spec/surveys/feature reports and all prior docs are unchanged; no README/CLAUDE.md/ARCHITECTURE registration was performed (convention for doc families); line parity with existing families is preserved (no edits to other docs). Quoted UI strings in non-English copies are verbatim English — consistent with the current i18n state (the release-flow keys are still pending in `langs/`).

## Next steps

- **User review & commit** of the working tree (nothing was staged or committed per project rule) — feature code and governance artifacts (ADRs, glossary, spec, surveys) are also still uncommitted.
- **Optional:** add the contextual-help route for the subcommand (`gitpr release -h` → `get_doc_url("release-notes.md")`) in `src/main.py` `HELP_MAP` in a future task — this family name is currently not wired to any code, unlike `suggested-reviewers.md`.
- **Optional:** translate the ~40 pending release-flow i18n keys in `langs/*.json` (known debt) — the docs already state the changelog headings follow the interface language with English fallback, so they will stay accurate when the keys land.
- **Optional:** register the new family in the README.md "Technical Documentation" index (and its 4 translated copies) in a later task — deliberately out of scope here.
