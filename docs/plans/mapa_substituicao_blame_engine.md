
### 🛠️ Mapa de Substituição (`src/blame_engine.py`)

Abaixo estão todas as substituições, incluindo o ajuste da lógica interna para `ORIGIN`.

| Função         | Linha Original (Português)                                                                                                                                                                                                                                                                       | Nova Linha (Inglês com `__()`)                                                                                                                                                                                                                                                                          |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `analyze...`   | `return {"status": "ORIGEM", "motivo": "Diff não encontrado (arquivo possivelmente criado aqui)."}`                                                                                                                                                                                              | `return {"status": "ORIGIN", "motivo": __("Diff not found (file possibly created here).")}`                                                                                                                                                                                                             |
| `analyze...`   | `return {"status": "ORIGEM", "motivo": "Sem chave de API. Assumindo origem."}`                                                                                                                                                                                                                   | `return {"status": "ORIGIN", "motivo": __("No API key. Assuming origin.")}`                                                                                                                                                                                                                             |
| `analyze...`   | `sys_inst = 'Você é um Arquiteto de Software. Analise o diff e determine se é a ORIGEM da regra (lógica nova) ou REFATORAÇÃO. Responda APENAS com JSON: {"status": "ORIGEM", "motivo": "Explique o que foi introduzido"} ou {"status": "REFATORACAO", "motivo": "Explique o que foi alterado"}'` | `sys_inst = __('You are a Software Architect. Analyze the diff and determine if it is the ORIGIN of the rule (new logic) or REFACTORING. Respond ONLY with JSON: {"status": "ORIGIN", "motivo": "Explain what was introduced"} or {"status": "REFACTORING", "motivo": "Explain what was changed"}')`    |
| `analyze...`   | `prompt = (\n        f"Analise o diff do commit {commit_hash} e retorne o JSON solicitado.\n\n"\n        f"DIFF:\n{diff[:4000]}" # Limitamos a 4000...` *(Altere a atribuição do prompt)*                                                                                                        | `prompt = (\n        __("Analyze the diff of commit {commit_hash} and return the requested JSON.\n\n", commit_hash=commit_hash) +\n        f"DIFF:\n{diff[:4000]}"\n    )`                                                                                                                              |
| `analyze...`   | `click.secho(f"  🤖 Consultando a IA ({api_model}) sobre o commit {commit_hash[:8]}...", fg="cyan", dim=True)`                                                                                                                                                                                    | `click.secho(__("  🤖 Consulting AI ({api_model}) about commit {commit_hash}...", api_model=api_model, commit_hash=commit_hash[:8]), fg="cyan", dim=True)`                                                                                                                                               |
| `analyze...`   | `return {"status": "ORIGEM", "motivo": "IA não retornou formato válido."}`                                                                                                                                                                                                                       | `return {"status": "ORIGIN", "motivo": __("AI did not return a valid format.")}`                                                                                                                                                                                                                        |
| `run_blame...` | `click.secho(f"\n🔍 Iniciando Arqueologia de Código...", fg="cyan", bold=True)`                                                                                                                                                                                                                   | `click.secho(__("\n🔍 Starting Code Archeology..."), fg="cyan", bold=True)`                                                                                                                                                                                                                              |
| `run_blame...` | `click.echo(f"📍 Arquivo: {file_path} (Linhas: {start_line} até {end_line})")`                                                                                                                                                                                                                    | `click.echo(__("📍 File: {file_path} (Lines: {start_line} to {end_line})", file_path=file_path, start_line=start_line, end_line=end_line))`                                                                                                                                                              |
| `run_blame...` | `click.secho("⚠️ Nenhum commit rastreável encontrado nestas linhas.", fg="yellow")`                                                                                                                                                                                                               | `click.secho(__("⚠️ No traceable commits found in these lines."), fg="yellow")`                                                                                                                                                                                                                          |
| `run_blame...` | `click.secho(f"✅ Encontrado(s) {len(initial_commits)} commit(s) na superfície. Iniciando viagem no tempo...\n", fg="green")`                                                                                                                                                                     | `click.secho(__("✅ Found {count} commit(s) on the surface. Starting time travel...\n", count=len(initial_commits)), fg="green")`                                                                                                                                                                        |
| `run_blame...` | `status = str(ai_analysis.get("status", "ORIGEM")).upper()`                                                                                                                                                                                                                                      | `status = str(ai_analysis.get("status", "ORIGIN")).upper()` *(Sem `__`, é lógica)*                                                                                                                                                                                                                      |
| `run_blame...` | `if status == "ORIGEM":`                                                                                                                                                                                                                                                                         | `if status == "ORIGIN":` *(Sem `__`, é lógica)*                                                                                                                                                                                                                                                         |
| `run_blame...` | `click.secho(f"\n📜 Histórico Consolidado da Regra (Linhas {start_line}-{end_line}):", fg="magenta", bold=True)`                                                                                                                                                                                  | `click.secho(__("\n📜 Consolidated Rule History (Lines {start_line}-{end_line}):", start_line=start_line, end_line=end_line), fg="magenta", bold=True)`                                                                                                                                                  |
| `run_blame...` | `cor = "green" if item["status"] == "ORIGEM" else "yellow"`                                                                                                                                                                                                                                      | `cor = "green" if item["status"] == "ORIGIN" else "yellow"` *(Sem `__`, é lógica)*                                                                                                                                                                                                                      |
| `run_blame...` | `icone = "👶" if item["status"] == "ORIGEM" else "🔧"`                                                                                                                                                                                                                                             | `icone = "👶" if item["status"] == "ORIGIN" else "🔧"` *(Sem `__`, é lógica)*                                                                                                                                                                                                                             |
| `run_blame...` | `click.secho(f"\n[{item['info']['date']}] {icone} {item['status']}: Por {item['info']['author']} (Commit: {item['hash']})", fg=cor, bold=True)`                                                                                                                                                  | `click.secho(__("\n[{date}] {icon} {status}: By {author} (Commit: {hash})", date=item['info']['date'], icon=icone, status=__(item['status']), author=item['info']['author'], hash=item['hash']), fg=cor, bold=True)`                                                                                    |
| `run_blame...` | `click.echo(f"   └─ Mensagem: \"{item['info']['message']}\"")`                                                                                                                                                                                                                                   | `click.echo(__("   └─ Message: \"{message}\"", message=item['info']['message']))`                                                                                                                                                                                                                       |
| `run_blame...` | `click.secho(f"   └─ Análise IA: {item['motivo']}", fg="cyan", dim=True)`                                                                                                                                                                                                                        | `click.secho(__("   └─ AI Analysis: {reason}", reason=item['motivo']), fg="cyan", dim=True)`                                                                                                                                                                                                            |
| `run_blame...` | `click.secho("📝 Gerando relatório Markdown unificado com o resumo da IA...", fg="cyan")`                                                                                                                                                                                                         | `click.secho(__("📝 Generating unified Markdown report with AI summary..."), fg="cyan")`                                                                                                                                                                                                                 |
| `run_blame...` | `md_content = f"# Linha do tempo da regra investigada\n\n"`                                                                                                                                                                                                                                      | `md_content = __("# Timeline of the investigated rule\n\n")`                                                                                                                                                                                                                                            |
| `run_blame...` | `md_content += f"**Arquivo:** \`{file_path}` (Linhas {start_line}-{end_line})\n\n"`                                                                                                                                                                                                              | `md_content += __("**File:** `{file_path}` (Lines {start_line}-{end_line})\n\n", file_path=file_path, start_line=start_line, end_line=end_line)`                                                                                                                                                        |
| `run_blame...` | `explicacao_ia = item['motivo'] if item['motivo'] else "Alteração identificada na regra"`                                                                                                                                                                                                        | `explicacao_ia = item['motivo'] if item['motivo'] else __("Change identified in the rule")`                                                                                                                                                                                                             |
| `run_blame...` | `summary_prompt = "Baseado na seguinte linha do tempo de commits de uma regra de negócio, escreva um único parágrafo resumindo a idade da regra, o autor original, o número de refatorações e deduza qual era a intenção original de negócio (o motivo real da regra existir no sistema).\n\n"`  | `summary_prompt = __("Based on the following commit timeline of a business rule, write a single paragraph summarizing the age of the rule, the original author, the number of refactorings, and deduce what the original business intention was (the real reason the rule exists in the system).\n\n")` |
| `run_blame...` | `sys_inst = "Você é um Arquiteto de Software. Gere APENAS um objeto JSON no formato {\"resumo\": \"texto do resumo\"}."`                                                                                                                                                                         | `sys_inst = __('You are a Software Architect. Generate ONLY a JSON object in the format {"resumo": "summary text"}.')`                                                                                                                                                                                  |
| `run_blame...` | `click.secho(f"  🤖 Consultando a IA ({api_model}) para o Resumo Executivo...", fg="cyan", dim=True)`                                                                                                                                                                                             | `click.secho(__("  🤖 Consulting AI ({api_model}) for the Executive Summary...", api_model=api_model), fg="cyan", dim=True)`                                                                                                                                                                             |
| `run_blame...` | `resumo_texto = summary_json.get("resumo", "Resumo não disponível.") if summary_json else "Resumo não disponível."`                                                                                                                                                                              | `resumo_texto = summary_json.get("resumo", __("Summary not available.")) if summary_json else __("Summary not available.")`                                                                                                                                                                             |
| `run_blame...` | `md_content += f"\n**Resumo:** {resumo_texto}\n"`                                                                                                                                                                                                                                                | `md_content += __("\n**Summary:** {summary}\n", summary=resumo_texto)`                                                                                                                                                                                                                                  |
| `run_blame...` | `click.secho(f"✅ Relatório unificado salvo com sucesso: '{output_filename}'", fg="green", bold=True)`                                                                                                                                                                                            | `click.secho(__("✅ Unified report successfully saved: '{output_filename}'", output_filename=output_filename), fg="green", bold=True)`                                                                                                                                                                   |
| `run_blame...` | `click.secho(f"❌ Erro ao salvar o relatório: {e}", fg="red")`                                                                                                                                                                                                                                    | `click.secho(__("❌ Error saving report: {error}", error=str(e)), fg="red")`                                                                                                                                                                                                                             |
