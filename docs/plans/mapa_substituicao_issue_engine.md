
### 🛠️ Mapa de Substituição (`src/issue_engine.py`)

Abaixo está a lista exata das linhas que devem ser alteradas no arquivo:

| Linha Aprox. | Linha Original (Português)                                                                                                                            | Nova Linha (Inglês com `__()`)                                                                                                                                                             |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `39`         | `click.secho("❌ Erro: Chave de API não encontrada.", fg="red")`                                                                                       | `click.secho(__("❌ Error: API Key not found."), fg="red")`                                                                                                                                 |
| `49`         | `sys_inst = "Você é um Arquiteto de Software. Siga o formato O Que / Por Que / Onde / Como para documentar a Issue."`                                 | `sys_inst = __("You are a Software Architect. Follow the What / Why / Where / How format to document the Issue.")`                                                                         |
| `52`         | `target_action = "documentar a evolução arquitetural, refatorações e a dívida técnica desta regra de negócio baseando-se no histórico de commits."`   | `target_action = __("document the architectural evolution, refactoring, and technical debt of this business rule based on the commit history.")`                                           |
| `53`         | `data_label = "LINHA DO TEMPO DA REGRA (DO MAIS ANTIGO PARA O MAIS NOVO):"`                                                                           | `data_label = __("RULE TIMELINE (FROM OLDEST TO NEWEST):")`                                                                                                                                |
| `55`         | `target_action = "documentar o Épico/Release detalhando todas as funcionalidades implementadas baseando-se no histórico integral da branch."`         | `target_action = __("document the Epic/Release detailing all implemented features based on the full branch history.")`                                                                     |
| `56`         | `data_label = "HISTÓRICO CONSOLIDADO DA BRANCH (COMMITS + PRS ANTIGOS):"`                                                                             | `data_label = __("CONSOLIDATED BRANCH HISTORY (COMMITS + OLD PRS):")`                                                                                                                      |
| `58`         | `target_action = "documentar a seguinte alteração de código recém introduzida."`                                                                      | `target_action = __("document the following recently introduced code change.")`                                                                                                            |
| `59`         | `data_label = "DIFF PARA ANÁLISE:"`                                                                                                                   | `data_label = __("DIFF FOR ANALYSIS:")`                                                                                                                                                    |
| `62-63`      | `prompt = (\n    f"Gere o objeto JSON solicitado seguindo as instruções de sistema para {target_action}\n\n"\n    f"{data_label}\n{context_text}"\n)` | `prompt = (\n    __("Generate the requested JSON object following the system instructions to {target_action}\n\n", target_action=target_action) +\n    f"{data_label}\n{context_text}"\n)` |
| `70`         | `click.secho("⚡ Resposta da Issue recuperada do cache local.", fg="green", dim=True)`                                                                 | `click.secho(__("⚡ Issue response retrieved from local cache."), fg="green", dim=True)`                                                                                                    |
| `73`         | `click.secho(f"🤖 Estruturando a Issue usando {provider.capitalize()} ({api_model})...", fg="cyan", dim=True)`                                         | `click.secho(__("🤖 Structuring Issue using {provider} ({api_model})...", provider=provider.capitalize(), api_model=api_model), fg="cyan", dim=True)`                                       |
| `81`         | `return {"titulo": "Erro ao gerar título", "corpo": "Não foi possível gerar o corpo da issue pela IA."}`                                              | `return {"titulo": __("Error generating title"), "corpo": __("Could not generate issue body by AI.")}`                                                                                     |
