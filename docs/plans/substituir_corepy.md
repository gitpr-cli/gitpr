# 🛠️ Mapa de Substituição (`src/core.py`)

| Função | Linha Original (Português) | Nova Linha (Inglês com `__()`) |
| --- | --- | --- |
| `get_git_diff` | `click.secho("⚠️ Aviso: O Git detectou novos arquivos que não estão sendo monitorados:", fg="yellow")` | `click.secho(__("⚠️ Warning: Git detected new untracked files:"), fg="yellow")` |
| `get_git_diff` | `click.secho("💡 Dica: Use 'git add <arquivo>' para que eles sejam incluídos na análise do GitPR.", fg="cyan")` | `click.secho(__("💡 Tip: Use 'git add <file>' to include them in the GitPR analysis."), fg="cyan")` |
| `get_git_diff` | `click.secho("📚 Entenda o motivo: https://github... \n", fg="blue", underline=True)` | `click.secho(__("📚 Understand why: https://github.com/gitpr-cli/gitpr.git/blob/main/docs/untracked-files.md\n"), fg="blue", underline=True)` |
| `get_git_diff` | `click.secho(f"❌ Erro ao executar o Git: {e.stderr}", fg="red")` | `click.secho(__("❌ Error running Git: {error}", error=e.stderr), fg="red")` |
| `get_git_diff` | `click.secho("❌ Git não encontrado. Certifique-se de que está instalado e no PATH.", fg="red")` | `click.secho(__("❌ Git not found. Make sure it is installed and in the PATH."), fg="red")` |
| `get_skill_context` | `click.secho(f"🧠 Arquivo {nome_arquivo} (Skill) encontrado e carregado!", fg="blue")` | `click.secho(__("🧠 File {file_name} (Skill) found and loaded!", file_name=nome_arquivo), fg="blue")` |
| `get_skill_context` | `click.secho(f"⚠️ Aviso: Falha ao ler o arquivo {nome_arquivo} ({e})", fg="yellow")` | `click.secho(__("⚠️ Warning: Failed to read file {file_name} ({error})", file_name=nome_arquivo, error=str(e)), fg="yellow")` |
| `generate_pr_...` | `click.secho("⚠️ Nenhum diff encontrado. Faça alguma alteração antes de rodar o comando.", fg="yellow")` | `click.secho(__("⚠️ No diff found. Make some changes before running the command."), fg="yellow")` |
| `generate_pr_...` | `instrucao_sistema = ... "Você é um especialista em Git. Gere mensagens de commit concisas."` | `instrucao_sistema = ... __("You are a Git expert. Generate concise commit messages.")` |
| `generate_pr_...` | `prompt = f"Gere APENAS um objeto JSON no formato {{\"commit_message\": \"...\"}} para este diff:\n{diff_text}"` | `prompt = __("Generate ONLY a JSON object in the format {json_format} for this diff:\n", json_format='{"commit_message": "..."}') + f"{diff_text}"` |
| `generate_pr_...` | `instrucao_sistema = ... "Você é um Arquiteto de Software Sênior. Foque em apontar melhorias."` | `instrucao_sistema = ... __("You are a Senior Software Architect. Focus on pointing out improvements.")` |
| `generate_pr_...` | `prompt = f"Gere APENAS um objeto JSON... para o código integral deste arquivo:\n{diff_text}"` | `prompt = __("Generate ONLY a JSON object in the format {json_format} with the analysis and improvements for the entire code of this file:\n", json_format='{"review": "..."}') + f"{diff_text}"` |
| `generate_pr_...` | `prompt = f"Gere APENAS um objeto JSON... apontando erros e melhorias para este diff:\n{diff_text}"` | `prompt = __("Generate ONLY a JSON object in the format {json_format} pointing out errors and improvements for this diff:\n", json_format='{"review": "..."}') + f"{diff_text}"` |
| `generate_pr_...` | `instrucao_sistema = ... "Você é um Tech Lead redigindo descrições de PR limpas e técnicas."` | `instrucao_sistema = ... __("You are a Tech Lead writing clean and technical PR descriptions.")` |
| `generate_pr_...` | `prompt = f"Gere APENAS um objeto JSON... para este diff:\n{diff_text}"` | `prompt = __("Generate ONLY a JSON object in the format {json_format} for this diff:\n", json_format='{"commit_message": "...", "pr_description": "..."}') + f"{diff_text}"` |
| `generate_pr_...` | `click.secho("⚡ Resposta recuperada do cache local.", fg="green", dim=True)` | `click.secho(__("⚡ Response retrieved from local cache."), fg="green", dim=True)` |
| `generate_pr_...` | `click.secho(f"❌ Erro: Chave de API para o provedor '{provider.capitalize()}' não encontrada.", fg="red")` | `click.secho(__("❌ Error: API Key for provider '{provider}' not found.", provider=provider.capitalize()), fg="red")` |
| `generate_pr_...` | `click.secho(f"❌ Erro: Não foi possível determinar o modelo para o provedor '{provider}'.", fg="red")` | `click.secho(__("❌ Error: Could not determine model for provider '{provider}'.", provider=provider), fg="red")` |
| `generate_pr_...` | `click.secho(f"🤖 O GitPR está analisando o seu código usando {provider.capitalize()} ({api_model})...\n", fg="cyan")` | `click.secho(__("🤖 GitPR is analyzing your code using {provider} ({model})...\n", provider=provider.capitalize(), model=api_model), fg="cyan")` |
| `generate_skill_..` | `click.secho("\n📥 Iniciando a configuração dos templates do GitPR...", fg="cyan", bold=True)` | `click.secho(__("\n📥 Starting GitPR templates configuration..."), fg="cyan", bold=True)` |
| `generate_skill_..` | `click.secho(f"⚠️ O arquivo {local_name} já existe neste diretório. Ele não será sobrescrito.", fg="yellow")` | `click.secho(__("⚠️ File {local_name} already exists in this directory. It will not be overwritten.", local_name=local_name), fg="yellow")` |
| `generate_skill_..` | `click.echo(f"A descarregar {local_name}...")` | `click.echo(__("Downloading {local_name}...", local_name=local_name))` |
| `generate_skill_..` | `click.secho(f"❌ Erro de rede ao baixar {local_name}: {e.reason}", fg="red")` | `click.secho(__("❌ Network error while downloading {local_name}: {error}", local_name=local_name, error=e.reason), fg="red")` |
| `generate_skill_..` | `click.secho(f"❌ Falha ao processar {local_name}: {e}", fg="red")` | `click.secho(__("❌ Failed to process {local_name}: {error}", local_name=local_name, error=str(e)), fg="red")` |
| `generate_skill_..` | `click.secho("\n✅ Templates base configurados com sucesso!", fg="green", bold=True)` | `click.secho(__("\n✅ Base templates successfully configured!"), fg="green", bold=True)` |
| `generate_skill_..` | `click.echo("Você pode agora abrir os arquivos gerados e personalizar o comportamento da ferramenta para o seu projeto:\n")` | `click.echo(__("You can now open the generated files and customize the tool's behavior for your project:\n"))` |
| `generate_skill_..` | `click.echo("  1. As regras de arquitetura para a IA no arquivo '.gitpr.pr.md' e '.gitpr.review.md'\n")` | `click.echo(__("  1. Architecture rules for AI in '.gitpr.pr.md' and '.gitpr.review.md'\n"))` |
| `generate_skill_..` | `click.echo("  2. As regras de regex locais no arquivo '.gitpr.linter.yml'\n")` | `click.echo(__("  2. Local regex rules in '.gitpr.linter.yml'\n"))` |
| `generate_skill_..` | `click.echo("\nNenhum arquivo novo foi baixado.")` | `click.echo(__("\nNo new files were downloaded."))` |
| `get_base_branch` | `click.secho("⚠️ Aviso: Branch principal remota não detectada. Assumindo 'main' como fallback padrão.", fg="yellow")` | `click.secho(__("⚠️ Warning: Remote main branch not detected. Assuming 'main' as default fallback."), fg="yellow")` |
| `get_git_...diff` | `click.secho("🔄 Sincronizando com o repositório remoto (git fetch)...", fg="cyan")` | `click.secho(__("🔄 Synchronizing with remote repository (git fetch)..."), fg="cyan")` |
| `get_git_...diff` | `click.secho(f"❌ Erro ao calcular o diff: {e.stderr}", fg="red")` | `click.secho(__("❌ Error calculating diff: {error}", error=e.stderr), fg="red")` |
| `install_hooks` | `click.secho("❌ Erro: Pasta .git não encontrada. Execute na raiz do projeto.", fg="red")` | `click.secho(__("❌ Error: .git folder not found. Run at the project root."), fg="red")` |
| `install_hooks` | `click.secho(f"📥 A descarregar {hook_name}...", fg="cyan")` | `click.secho(__("📥 Downloading {hook_name}...", hook_name=hook_name), fg="cyan")` |
| `install_hooks` | `click.secho(f"⚠️ Falha ao instalar {hook_name}: {e}", fg="yellow")` | `click.secho(__("⚠️ Failed to install {hook_name}: {error}", hook_name=hook_name, error=str(e)), fg="yellow")` |
| `get_branch...` | `click.secho(f"🔄 Compilando historico do repositorio '{repo_name}', branch '{branch}' contra '{base_branch}'...", fg="cyan")` | `click.secho(__("🔄 Compiling history of repository '{repo_name}', branch '{branch}' against '{base_branch}'...", repo_name=repo_name, branch=branch, base_branch=base_branch), fg="cyan")` |
| `get_branch...` | `hybrid_context = f"Repositorio: {repo_name}\nResumo Historico da Branch: {branch}\n\n"` | `hybrid_context = __("Repository: {repo_name}\nBranch History Summary: {branch}\n\n", repo_name=repo_name, branch=branch)` |
| `get_branch...` | `hybrid_context += "=== COMMITS REGISTRADOS ===\n"` | `hybrid_context += __("=== REGISTERED COMMITS ===\n")` |
| `get_branch...` | `hybrid_context += "Nenhum commit exclusivo encontrado nesta branch.\n\n"` | `hybrid_context += __("No exclusive commits found in this branch.\n\n")` |
| `get_branch...` | `click.secho(f"⚠️ Aviso: Não foi possível obter o Git Log: {e.stderr}", fg="yellow")` | `click.secho(__("⚠️ Warning: Could not get Git Log: {error}", error=e.stderr), fg="yellow")` |
| `get_branch...` | `hybrid_context += "=== HISTÓRICO DE PRs DA IA ===\nNenhum PR anterior gerado por IA encontrado em cache para esta branch.\n"` | `hybrid_context += __("=== AI PR HISTORY ===\nNo previous AI-generated PR found in cache for this branch.\n")` |
